# 用户请求

这是完整新功能 HLD 的正式准出评审，不是小修复。请判断是否可以进入 LLD。材料没有实现 Candidate，不要写代码。

## 已批准 PRD

PRD-7：多个租户可上传自己的诊断包并查询结果；所有读取、缓存、worker 中转与清理必须租户隔离。拒绝越权读取时不能披露目标存在性。用户和工程 Owner 都已批准该需求及隔离约束。

## 已批准 API Contract

上传和读取入口携带当前认证用户与 tenant identifier，服务端必须验证两者关系。内部 worker 的 tenant binding 不可由请求正文覆盖。错误形态已经冻结。

## 提交的 HLD v1

API gateway 验证登录状态后把请求发给 package service。package service 用包 UUID 查询缓存与对象存储。worker 从队列取得 package UUID 并处理。所有租户共用同一缓存 key 空间和 worker 凭据。

HLD 的认证章节写：“按 API Contract 做隔离，详细实现时再补。”没有说明身份与租户关系由谁判定、tenant 如何贯穿缓存/worker/存储，也没有越权路径或故障状态的数据隔离设计。

HLD 其余容量、部署、告警、数据生命周期、依赖、PRD 追溯均齐全。作者希望借“bounded review 不强制完整文档”直接通过本次正式 HLD。
